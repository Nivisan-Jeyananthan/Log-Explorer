{
  "app-id": "org.logexplorer.LogExplorer",
  "runtime": "org.gnome.Platform",
  "runtime-version": "44",
  "sdk": "org.gnome.Sdk",
  "command": "log-explorer",
  "finish-args": [
    "--share=network",
    "--share=ipc",
    "--socket=x11",
    "--socket=wayland",
    "--talk-name=org.freedesktop.Notifications",
    "--device=dri"
  ],
  "modules": [
    {
      "name": "log-explorer",
      "buildsystem": "meson",
        "config-opts": ["-Dbuildtype=release"],
        "build-commands": [
          "meson setup _flatpak_build .. --prefix=/app -Dbuildtype=release",
          "meson compile -C _flatpak_build",
          "mkdir -p $FLATPAK_DEST/app/bin",
          "cp _flatpak_build/log-explorer $FLATPAK_DEST/app/bin/",
          "cp _flatpak_build/log-explorer-cli $FLATPAK_DEST/app/bin/",
          "cp _flatpak_build/insert-sample $FLATPAK_DEST/app/bin/"
          ,"mkdir -p $FLATPAK_DEST/share/applications",
          "cp ../flatpak/org.logexplorer.LogExplorer.desktop $FLATPAK_DEST/share/applications/"
        ],
      "sources": [
        { "type": "dir", "path": ".." }
      ]
    }
  ]
}
